[tool.ruff]
line-length = 120
target-version = "py312"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# UP017: datetime.UTC は 3.11+。ローカル開発の 3.10 互換のため timezone.utc を維持。
ignore = ["E501", "UP017"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
